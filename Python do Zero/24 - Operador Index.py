# Operador Index:

name = "daniel Paladino!"

#if(name[0].islower()):
    #name = name.capitalize()  # Essa linha de código transforma o primeiro caractere do nome de minusculo para maiusculo.

first_name = name[0:6].upper()
last_name = name[6:].lower()
last_character = name[-1]

print(first_name)
print(last_name)
print(last_character)